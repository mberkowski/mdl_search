import React from 'react'
import BorealisPDF from 'borealis-pdf'

export default (props) => {
  console.log(props.config['pdf']);
  return <BorealisPDF {...props.config['pdf']} basename={props.basename} />
}